<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssm.blog.dao.ArticleDao">
    <!--目的:为dao接口方法提供sql语句配置
    即针对dao接口中的方法编写我们的sql语句-->
    <resultMap id="articleMap" type="com.ssm.blog.entity.Article">
        <id column="articleId" property="articleId"/>
        <result column="title" property="title"/>
        <result column="saveTime" property="saveTime"/>
        <result column="publishTime" property="publishTime"/>
        <result column="summaryContent" property="summaryContent"/>
        <result column="markdownContent" property="markdownContent"/>
        <result column="htmlContent" property="htmlContent"/>
        <collection property="categories" ofType="com.ssm.blog.entity.Category">
            <id column="categoryId" property="categoryId"/>
            <result column="categoryName" property="categoryName"/>
        </collection>
        <collection property="tags" ofType="com.ssm.blog.entity.Tag">
            <id column="id" property="id" />
            <result column="tagName" property="tagName" />
            <result column="articleId" property="articleId" />
        </collection>
    </resultMap>

    <insert id="add" parameterType="Article">
        INSERT INTO
            article(articleId,saveTime,publishTime,title,summaryContent,markdownContent,htmlContent)
        VALUES
            (#{article.articleId},#{article.saveTime},#{article.publishTime},#{article.title},#{article.summaryContent},#{article.markdownContent},#{article.htmlContent})

        ;INSERT INTO
            article_category(article_id,category_id)
        VALUES
            <foreach collection="article.categories" item="category" separator=",">
                (#{article.articleId},#{category.categoryId})
            </foreach>

        ;INSERT INTO
            tag(tagName,articleId)
        VALUES
            <foreach collection="article.tags" item="tag" separator=",">
                (#{tag.tagName},#{article.articleId})
            </foreach>
    </insert>

    <select id="get" resultMap="articleMap" parameterType="java.lang.Long">
        SELECT
            *
        FROM
            article a
        LEFT JOIN
            tag t
        ON
           a.articleId = t.articleId
        LEFT JOIN
            article_category a_t
        ON
            a.articleId = a_t.article_id
        LEFT JOIN
            category c
        ON
            a_t.category_id = c.categoryId
        WHERE
            a.articleId = #{articleId}
    </select>

    <select id="getTotalCount" resultType="java.lang.Integer" parameterType="java.lang.String">
        SELECT
            count(*)
        FROM
            article a
        <where>
            <if test="searchParam != null and searchParam != ''"> a.title like '%' #{searchParam} '%'</if>
        </where>

    </select>

    <select id="list" resultMap="articleMap">
        SELECT
            *
        FROM
            (
                SELECT * FROM
                    article
                <where><if test="searchParam != null and searchParam != ''"> article.title like '%' #{searchParam} '%'</if></where> ORDER BY article.publishTime Desc
                LIMIT #{offsetCount}, #{pageSize}
            ) a
        LEFT JOIN
            article_category a_t
        ON
            a.articleId = a_t.article_id
        LEFT JOIN
            category t
        ON
            t.categoryId = a_t.category_id
    </select>

    <select id="ifExistTitle" parameterType="java.lang.String" resultType="java.lang.Integer">
        SELECT
            count(*)
        FROM
            article a
        WHERE
            a.title = #{title}
    </select>
</mapper>